import { reactVersionTester } from './react-version-tester';

describe('reactVersionTester', () => {
  it('should work', () => {
    expect(reactVersionTester()).toEqual('react-version-tester');
  });
});
